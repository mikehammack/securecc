package baubles.api;

import baubles.api.cap.IBaublesItemHandler;
import net.minecraft.entity.player.EntityPlayer;

/**
 * Compile-only stub for the Baubles API. The real class comes from the
 * Baubles mod at runtime; this is never packaged. Only the methods
 * SecureCC calls are declared.
 */
public class BaublesApi {
    public static IBaublesItemHandler getBaublesHandler(EntityPlayer player) {
        throw new UnsupportedOperationException("stub");
    }
}

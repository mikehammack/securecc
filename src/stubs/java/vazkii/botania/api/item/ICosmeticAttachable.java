package vazkii.botania.api.item;

/**
 * Compile-only stub for the Botania API. Plethora declares
 * {@code ItemNeuralInterface implements ICosmeticAttachable} behind
 * {@code @Optional.Interface}, so javac needs the type to resolve the
 * supertype hierarchy, but it is never packaged into the mod jar and never
 * loaded at runtime (Botania itself provides the real interface).
 */
public interface ICosmeticAttachable {
}
